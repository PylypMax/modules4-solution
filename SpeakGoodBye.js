(function speak(name) {
  var speakWord = "Good Bye";
  console.log(speakWord + " " + name);
})();

var speakWord = "Good Bye";

var byeSpeaker = {
  speak : function speak(name) {
    var speakWord = "Good Bye";
    console.log(speakWord + " " + name);
  }
}

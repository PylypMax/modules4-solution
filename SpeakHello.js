
(function speak(name) {
  var speakWord = "Hello";
  console.log(speakWord + " " + name);
})();

var speakWord = "Hello";

var helloSpeaker = {
  speak : function speak(name) {
    var speakWord = "Hello";
    console.log(speakWord + " " + name);
  }
}

